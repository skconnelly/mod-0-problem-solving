// Write a method or function that removes all instances of 
// the letter "s" in a string. 
// The method or function should accept a string as an argument and 
// return the same string with every instance of the letter 
// "s" removed.

function removeS(stringHere) {
        console.log(stringHere.replaceAll("s", ""));
    };

removeS("pineapples sara squidward.")

